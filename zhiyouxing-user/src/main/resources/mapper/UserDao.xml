<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zhiyouxing.user.dao.UserDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.zhiyouxing.user.entity.UserEntity" id="userMap">
        <result property="userAccount" column="user_account"/>
        <result property="password" column="password"/>
        <result property="userName" column="user_name"/>
        <result property="avatar" column="avatar"/>
        <result property="gender" column="gender"/>
        <result property="contactPhone" column="contact_phone"/>
        <result property="status" column="status"/>
        <result property="passwordWrongNum" column="password_wrong_num"/>
    </resultMap>

	<select id="selectListVO"
		resultType="com.zhiyouxing.user.entity.vo.UserVO" >
		SELECT * FROM user  user         
        ${ew.customSqlSegment}
	</select>
	
	<select id="selectVO"
		resultType="com.zhiyouxing.user.entity.vo.UserVO" >
		SELECT  user.* FROM user  user 	
 		${ew.customSqlSegment}
	</select>

    <select id="selectListView"
		resultType="com.zhiyouxing.user.entity.view.UserView" >

		SELECT  user.* FROM user  user 	        
        ${ew.customSqlSegment}
	</select>
	
	<select id="selectView"
		resultType="com.zhiyouxing.user.entity.view.UserView" >
		SELECT * FROM user  user ${ew.customSqlSegment}
	</select>
	


</mapper>
