<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zhiyouxing.common.dao.StoreupDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.zhiyouxing.common.entity.StoreupEntity" id="storeUpMap">
        <result property="userId" column="user_id"/>
        <result property="refId" column="ref_id"/>
        <result property="tableName" column="table_name"/>
        <result property="name" column="name"/>
        <result property="picture" column="picture"/>
        <result property="type" column="type"/>
        <result property="intelType" column="intel_type"/>
        <result property="remark" column="remark"/>
    </resultMap>

	<select id="selectListVO"
		resultType="com.zhiyouxing.common.entity.vo.StoreupVO" >
		SELECT * FROM store_up  store_up         
        ${ew.customSqlSegment}
	</select>
	
	<select id="selectVO"
		resultType="com.zhiyouxing.common.entity.vo.StoreupVO" >
		SELECT  store_up.* FROM store_up  store_up 	
 		${ew.customSqlSegment}
	</select>

    <select id="selectListView"
		resultType="com.zhiyouxing.common.entity.view.StoreupView" >

		SELECT  store_up.* FROM store_up  store_up 	        
        ${ew.customSqlSegment}
	</select>
	
	<select id="selectView"
		resultType="com.zhiyouxing.common.entity.view.StoreupView" >
		SELECT * FROM store_up  store_up ${ew.customSqlSegment}
	</select>
	


</mapper>
